# Raven Mail 系统配置模板

# --- 核心模式设置 ---
# 文电正文编辑模式，可选值: 
#   text:       纯文本模式（最轻量，无外部依赖）
#   rich:       富文本模式（支持 HTML 格式，预留接口）
#   onlyoffice: ONLYOFFICE 文档办公模式（功能最强，需配置下方服务器）
VITE_MAIL_CONTENT_MODE=text

# --- 后端服务配置 ---
# Raven 后端 API 地址。
# 注意：在 ONLYOFFICE 模式下，此处必须填写 ONLYOFFICE 服务器能够访问到的物理 IP 或主机名
# 禁止在生产联调时使用 localhost，否则会导致“无法保存”或“文件下载失败”
VITE_BACKEND_URL=http://localhost:8080

# --- ONLYOFFICE 服务器配置 ---
# ONLYOFFICE Document Server 的访问地址（需带末尾斜杠）
VITE_ONLYOFFICE_SERVER=http://your-onlyoffice-server:8090/
